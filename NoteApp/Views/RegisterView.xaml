<Window x:Class="NoteApp.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="注册" Height="550" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="用户注册" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" FontFamily="楷体"/>
            <TextBlock Text="账户名" Margin="0,0,0,5"/>
            <TextBox Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
            <TextBlock Text="密码" Margin="0,10,0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="30" Padding="5"/>
            <TextBlock Text="确认密码" Margin="0,10,0,5"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Height="30" Padding="5"/>
            <TextBlock Text="电话" Margin="0,10,0,5"/>
            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
            <TextBlock Text="地址" Margin="0,10,0,5"/>
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="注册" Width="100" Height="37" Command="{Binding RegisterCommand}" Margin="0,0,10,0" Style="{StaticResource RoundedButtonStyle}"/>
                <Button Content="取消" Width="100" Height="37" Command="{Binding CancelCommand}" Style="{StaticResource RoundedButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>