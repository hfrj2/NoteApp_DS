<UserControl x:Class="NoteApp.Views.NoteManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧便签列表 -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="新增" Width="80" Margin="0,0,10,0" Command="{Binding AddCommand}"/>
                <Button Content="刷新" Width="80" Command="{Binding RefreshCommand}"/>
            </StackPanel>
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Notes}"
                      SelectedItem="{Binding SelectedNote}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="标题" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- 右侧编辑区 -->
        <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="15">
            <StackPanel>
                <TextBlock Text="便签编辑" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                <TextBlock Text="标题" Margin="0,0,0,5"/>
                <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5" VerticalContentAlignment="Center"/>
                <TextBlock Text="内容" Margin="0,10,0,5"/>
                <TextBox Text="{Binding EditContent, UpdateSourceTrigger=PropertyChanged}" Height="200" Padding="5" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                <TextBlock Text="创建时间" Margin="0,10,0,5"/>
                <TextBox Text="{Binding EditCreatedAt, StringFormat=yyyy-MM-dd HH:mm:ss}" IsReadOnly="True" Height="30" Padding="5" VerticalContentAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="保存" Width="80" Margin="0,0,10,0" Command="{Binding SaveCommand}"/>
                    <Button Content="删除" Width="80" Command="{Binding DeleteCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>