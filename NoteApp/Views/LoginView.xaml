<!-- Views/LoginView.xaml -->
<Window x:Class="NoteApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:conv="clr-namespace:NoteApp.Converters"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="便签管理系统 - 登录" 
        Height="550" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid Background="#2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Border Grid.Row="0" Background="#34495E" Padding="20">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="📝 便签管理系统" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="White"/>
                <TextBlock Text="请登录您的账户" 
                           FontSize="14" 
                           Foreground="#BDC3C7" 
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 登录表单 -->
        <Grid Grid.Row="1" Margin="40,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 用户名 -->
            <StackPanel Grid.Row="0" Margin="0,20">
                <Label Content="👤 用户名" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="UsernameBox"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Background="#34495E"
                         Foreground="White"
                         BorderBrush="#5D6D7E"
                         FontSize="14"
                         Height="35"
                         Padding="10"
                         IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
            </StackPanel>

            <!-- 密码 -->
            <StackPanel Grid.Row="1" Margin="0,10">
                <Label Content="🔒 密码" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             KeyDown="PasswordBox_KeyDown"
                             Background="#34495E"
                             Foreground="White"
                             BorderBrush="#5D6D7E"
                             FontSize="14"
                             Height="35"
                             Padding="10"
                             IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
            </StackPanel>

            <!-- 错误信息 -->
            <TextBlock Grid.Row="2" 
                       Text="{Binding ErrorMessage}" 
                       Foreground="#E74C3C"
                       FontSize="13"
                       Margin="0,10,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- 按钮 -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,30,0,0">
                <Button Content="登录"
                        Command="{Binding LoginCommand}"
                        Background="#27AE60"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Width="130"
                        Height="45"
                        Margin="5"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#219A52"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1B7A41"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="注册"
                        Command="{Binding RegisterCommand}"
                        Background="#3498DB"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Width="130"
                        Height="45"
                        Margin="5"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2980B9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1F618D"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- 加载中 -->
            <StackPanel Grid.Row="4" 
                        HorizontalAlignment="Center"
                        Margin="0,20,0,0"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar IsIndeterminate="True" Width="200" Height="5" Foreground="#3498DB"/>
                <TextBlock Text="正在登录..." Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 底部 -->
        <Border Grid.Row="2" Background="#34495E" Padding="10">
            <TextBlock Text="© 2024 便签管理系统 v1.0" 
                       Foreground="#7F8C8D" 
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>