<Window x:Class="NoteApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="登录" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="便签程序登录" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock Text="账户名" Margin="0,0,0,5"/>
            <TextBox Text="{Binding AccountName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
            <TextBlock Text="密码" Margin="0,10,0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="30" Padding="5"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                <Button Content="登录" Width="100" Height="30" Command="{Binding LoginCommand}" Margin="0,0,10,0"/>
                <Button Content="注册" Width="100" Height="30" Command="{Binding GoToRegisterCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>